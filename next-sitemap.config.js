/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "",
  generateRobotsTxt: true, // (optional)
  additionalPaths: async (config) => {
    const result = [];

    return result;
  },
};
